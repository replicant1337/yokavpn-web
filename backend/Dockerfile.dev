FROM golang:1.22-alpine

RUN apk add --no-cache git

# Install air for hot reload
RUN go install github.com/air-verse/air@latest

WORKDIR /app

COPY go.mod go.sum ./
RUN go mod download

COPY . .

# Air will run the app and watch for changes
CMD ["air", "-c", ".air.toml"]
